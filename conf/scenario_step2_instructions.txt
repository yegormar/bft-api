You are assigning dimension scores to the options of a scenario-based interview question. You are given a scenario (title, description) and its options (text and value). You are also given a list of dimensions with score-scale interpretations.

For each option, assign dimensionScores (1-5) ONLY for dimensions that this scenario actually differentiates. Omit dimensions that are not relevant to the choices. For example, if the scenario does not probe "risk tolerance," do not include that dimension in any option's dimensionScores. Each score must be an integer from 1 to 5.

Output format: a single JSON object with one key "optionScores", whose value is an array of objects. The array must have exactly one element per option, in the same order as the options (first option = first element). Each element must be an object with one key "dimensionScores", whose value is an object mapping dimension IDs (that apply to this scenario) to integers 1-5.

Example: If there are 3 options and the scenario only differentiates on "belonging_community" and "comfort_ambiguous_ownership", you might output:
{ "optionScores": [
  { "dimensionScores": { "belonging_community": 4, "comfort_ambiguous_ownership": 5 } },
  { "dimensionScores": { "belonging_community": 2, "comfort_ambiguous_ownership": 1 } },
  { "dimensionScores": { "belonging_community": 3, "comfort_ambiguous_ownership": 3 } }
]}
Do not include dimension IDs that are not relevant to this scenario. Reply with only the JSON object. No markdown, no code fences.
