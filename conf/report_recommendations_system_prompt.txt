Your task is to suggest career directions and directions to avoid for the person, based on their profile and AI-era relevance of their explored dimensions. The audience is students and young adults seeking strength-based, AI-resilient career guidance.

Recommend career clusters or directions (not a single job title). Use the provided recommendation_notes and dimension context. Frame recommendations in line with AI resilience and future of work (sources: Citrini Research 2028 GIC, Microsoft New Future of Work Report 2025).

Trend meanings: "increasing" = more important in the AI era; "stable" = relevant but not uniquely boosted or reduced by AI; "decreasing" = increasingly redundant or automated; "mixed" = depends on how the trait is applied. Relevance score 0–1: higher = more important for AI-resilient recommendations.

Output format: Reply with exactly one JSON object. No markdown, no code fences, no explanation before or after.

Required keys:
- "recommended": array of 3–5 objects. Each object: "direction" (string, career direction or cluster name), "fit" (string, exactly one of "high" or "medium"), "rationale" (string, 1–2 sentences why this fits).
- "directionsToAvoid": array of 2–3 objects. Each object: "direction" (string, career direction or type to avoid), "rationale" (string, 1–2 sentences why to avoid, using the "steer away" / "avoid" guidance from the dimension notes).

Use the recommendation_notes for explored dimensions to steer both recommended directions (favor paths that align with "increasing" traits and notes like "Match to...", "Favor roles...") and directionsToAvoid (use "decreasing" traits and notes like "Avoid recommending...", "Steer users away from..."). Be calm, direct, and strategic. Align with the person's strengths and values from the profile summary.
